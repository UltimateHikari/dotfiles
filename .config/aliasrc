# Verbosity and settings that you pretty much just always are going to want.
alias \
	la="ls -lHa" \
	bat="cat /sys/class/power_supply/BAT?/capacity" \
	cp="cp -iv" \
	mv="mv -iv" \
	rm="rm -iv" \
	mkd="mkdir -pv" \
	ffmpeg="ffmpeg -hide_banner"

# Colorize commands when possible.
alias \
	ls="ls -hN --color=auto --group-directories-first" \
	grep="grep --color=auto" \
	diff="diff --color=auto" \
	ccat="highlight --out-format=ansi"

# Productivity shortcuts
alias tsm="transmission-daemon && tremc"
alias musicsync="sudo rsync -rlptDzv --size-only --delete-delay --progress /media/AndyArchive/Music/  /mnt/MusicOnCayin/"
alias config='/usr/bin/git --git-dir=$HOME/dotfiles --work-tree=$HOME'
alias realias='. ~/.config/zsh/.zshrc'
alias memocom='nvim ~/AndyData/commands'

# Programming shortcuts
alias vivado='/home/andy/Bloat/Vivado/Vivado/2019.2/bin/vivado -log /home/andy/Bloat/Vivado/vivado.log -journal /home/andy/Bloat/Vivado/vivado.jou'
alias cbuild='cd ./build && cmake .. && make; cd ..'
alias evmssh='ssh -vv -Y -i ~/.ssh/evmpu -p 2222 evmpu@84.237.52.21'
alias oppssh='ssh -vv -Y -i ~/.ssh/opp -p 2222 opp@84.237.52.21'
evmsend(){ 
 scp -i ~/.ssh/evmpu -P 2222 "$1" evmpu@84.237.52.21:/home/evmpu/19201/Rudometov"$2"
}
evmget(){
 scp -i ~/.ssh/evmpu -P 2222 evmpu@84.237.52.21:/home/evmpu/19201/Rudometov"$1" "$2"
}
alias toevm='~/Code/2020_seminars/evm/lab9'
alias ccfitssh='luit -encoding KOI8-R ssh a.rudometov@ccfit.nsu.ru'
alias clussh='ssh -X hpcuser143@clu.nusc.ru'
#alias tosys="~/Code/2021/systems/lab34m"
alias tosb="~/Code/2021/sberlab/knative"
alias gb="go build -o ../bin/$1"
alias k="kubectl"
ff(){
    flowc1 jar=1 $1.flow && java -jar $1.jar
}
alias gtoken="cat ~/AndyData/githubtoken"
alias gprep="ssh-add ~/.ssh/github_ed25519"
alias touni="cd /media/AndyArchive/10_Arch_uni_Backup/3Grade; ranger"
alias snake="cd ~/Code/2021/net/SnakeCursesClient/target"
alias copyschema="sudo cp ~/Code/2021/oooad/fairlabs/postgres/schema.sql /var/lib/postgres/"
alias wiki="gollum ~/Code/hikari-wiki/"
alias kssh="ssh -v -i ~/Code/2022/sberlab_keys/KeyPair-rudometov.pem root@46.243.142.144"
